# 教師管理指南

## Google 資源

| 資源             | 連結／用途                                                                                                                   |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 主試算表         | [歷史互動探索館｜學生成績主試算表](https://docs.google.com/spreadsheets/d/19FUB6aC_zmRdXz-VY9n3ZHY7NTOzwJIq0lh-Ucfhs-8/edit) |
| 成績 API         | `https://script.google.com/macros/s/AKfycbymowbPxS3_LxAcfOP546HahoNKV1S-8Uwatj0-0Uw3Rz4oYMVMK0VeJAiG17BCzmWG/exec`           |
| 成績 Apps Script | [程式專案](https://script.google.com/u/1/home/projects/1jEmaJmCGJ4gFx8ueWWc_mN3IUODpuDQOBBe-5IYJo0ccUjHlNciCGW9k/edit)       |

當學生完成任務測驗，網站會把逐題答案加入本機同步佇列，再傳送至成績 API。這不包含嵌入遊戲的內部成績。新版 GAS 以 `HQ_` 分頁儲存答案、名單及題目；相同提交識別碼重試不會重複寫入。離線時保留在原裝置並自動重試。**本專案的 Google 服務已升級；網站改動發佈後即可使用新版功能。** 詳見 [教學後台升級記錄](./TEACHING_UPGRADE.md)。

## 新增或修改內容

### 新增的日常管理功能

教師後台右上角的「網站與教材管理」會進入 CMS。成績 PIN 與 CMS 的 GitHub 登入權限分開。

| CMS 項目                        | 可自行修改                                                                                                         |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| 網站與首頁設定 → 網站與首頁區塊 | 網站名稱、副標題、英文名、標誌、大圖、報到標題、精選標題、沒有任務時的提示；主題卡片、每日一問、積分區塊的顯示開關 |
| 網站與首頁設定 → 首頁主題卡片   | 卡片名稱、簡介、符號、顏色、連結年級；拖動排序，最多六張                                                           |
| 網站與首頁設定 → 年級顯示及排序 | 中一至中六的顯示名稱、顯示開關及選單次序；拖動排序                                                                 |
| 年級與課題                      | 新增課題、修改名稱、所屬年級、排序、公開顯示                                                                       |
| 歷史任務                        | 原有教材欄位，以及所屬課題、公開顯示、首頁精選、顯示次序                                                           |

課題識別碼建立後請保持不變，改名稱只改「課題名稱」。任務年級會跟隨課題，改名或調整所屬年級不會改動任務識別碼，既有成績仍用原有任務識別碼對應。課題停用請用隱藏，避免刪除後留下無效任務連結。

新任務預設隱藏且不是首頁精選。公開任務、課題及年級三者都開啟時，學生才看得到。首頁只列精選任務；「全部」列出所有公開任務；課題按鈕會真正篩選該課題。次序數字越小越前，同數字按標題排列。隱藏只影響網站顯示，不是存取權限；不要把私人資料放進公開教材檔案。

「每日一問」目前是可修改的展示文字，不會自動每日更換或收集答案。主題卡片連到年級；隱藏年級會一併隱藏其主題卡片、課題及任務。

### 儲存、檢查與復原

儲存後會觸發 GitHub Actions。建置成功後重新整理正式網站，檢查首頁、課題選單及任務。系統會檢查課題連結、重複識別碼、必要欄位及答案範圍，錯誤會阻止新版發佈，原先已發佈的網站仍保留。首頁及任務編輯器可在儲存前預覽目前草稿及試答，其他內容使用目前建置版本；正式網站仍要等待發佈。

修改前可先記下原值或保存圖片。需要復原時可在 CMS 填回原值再儲存；較大範圍可由有儲存庫權限的人利用 GitHub 提交紀錄復原對應變更。復原同樣需要等待重新建置。請勿隨意更改已有任務的識別碼。

開發檢查：`pnpm check`、`pnpm test:content`、`pnpm build`。本機以 `pnpm dev` 啟動後，可用 `/?preview=student` 查看學生版面；此開發預覽不建立學生資料，請勿用來驗證成績提交。

登入網站 `/cms/` 後，選擇 **Sign in with Token**，再使用只限於 `history-quest` 儲存庫的 GitHub fine-grained personal access token 登入。選擇「歷史任務」後即可新增或修改內容。`task_id` 只可使用英文字母、數字、底線及連字號，發佈後不應更改，否則舊成績不再對應。新增／複製任務首次儲存會自動加入獨立後綴並隱藏；確認內容後再公開。完成編輯並發佈後，Sveltia CMS 會 Commit 至 `main`，GitHub Actions 隨即重新建置 GitHub Pages。詳細安全設定見 [內容管理登入方式](./CMS_ACCESS.md)。

## 影片與互動遊戲

YouTube 連結會轉換為 `youtube-nocookie.com` 嵌入網址；Google Drive 檔案連結會轉為預覽模式。HTML5／iframe 遊戲必須使用 HTTPS，而且來源網站需允許被 iframe 嵌入。遊戲 iframe 會套用 sandbox 權限限制，以減少第三方內容風險。

中五「冷戰世界」的《冷戰・地下檔案》保持獨立部署，更新及成績範圍見 [冷戰遊戲嵌入維護](./COLD_WAR_GAME.md)。

## 教師後台

### 編輯室優化

- 任務欄位分為「基本資料、封面與取景、教材內容、測驗題目、發布設定」，可收合切換。原有 JSON 檔仍維持原格式，任務識別碼不變。
- 表單頂部顯示必要內容、封面、連結格式及答案範圍問題；修正後再儲存。外部影片／遊戲是否允許嵌入，仍須用學生預覽實際確認。
- 學生預覽可切換首頁卡片、全文試答，以及 390px、768px、1280px 寬度。桌面預覽在窄編輯欄內可水平捲動。
- 「教材搜尋與圖片目錄」可按年級、課題、公開狀態及關鍵字搜尋，按標題或顯示次序排序，再進入 CMS 編輯。CMS 任務清單亦可按課題分組及公開狀態篩選；要調整學生看到的次序，修改發布設定內的數字。課題清單支援拖動排序。
- 圖片檢查顯示尺寸、低解像度／過大尺寸提醒及使用位置；新上載限制為 5 MiB。使用清單涵蓋上次建置的首頁、封面、Markdown 插圖及素材目錄，不代表尚未儲存的草稿。
- 編輯器可下載 JSON 備份、開啟 GitHub 版本紀錄及發布進度。還原時先回到一份已儲存、有效的教材，匯入同一任務的 JSON 備份，檢查變更清單和學生預覽，再按儲存。待還原時不要同時編輯下方欄位；可按「取消還原」繼續一般編輯。新任務應先建立，複製請用 CMS 的複製功能。還原不修改學生成績。
- 教師後台提供「開啟 Google 成績表」；登入成績後會顯示舊版或新版連線狀態。試算表仍需原有 Google 帳戶權限。

正式 CMS 仍使用 GitHub。`127.0.0.1` 的「使用本機倉庫」只供開發；開發者可在本機 CMS 網址加 `?test=1` 使用隔離測試倉庫（只存於瀏覽器），不會同步 GitHub 或 Google。此參數在正式網站不啟用。

2026-09-24 已完成 Google 升級：正式 API 回報 `version: 2.1.0`、`api_version: 2`，原網址及教師 PIN 保持有效。原有 3 筆成績已保留，4 個公開任務已同步；獨立 Google 測試表的交卷、批改、評語、名冊及舊網站相容測試全部通過。網站優化仍需自行 commit／push 才會出現在正式網站。詳見 [升級記錄及日常操作](./TEACHING_UPGRADE.md)。

前往 `/admin`，輸入交付時提供的教師 PIN。PIN 驗證在 GAS 執行，並非只以 JavaScript 隱藏畫面。成功後可檢視不重複學生、完成紀錄、任務分頁數量、平均分圖表及逐筆資料。

新版包含答案收集箱、成績與逐題答案 CSV 匯出、班級名單匯入及欠交比對、短答批改與評語。學生由「我的提交與評語」使用私人查閱碼查看結果。每次改題發佈後，重新載入教師工作室網頁，會自動更新提交、學生名單，先同步題目版本再核算本次載入的未核算提交。頂部「重新整理」會重新讀取目前已載入的頁數並重試失敗項目；「載入更早的 100 份」會處理新載入頁面的未核算提交。未載入提交不包含在本次核算內，已有成績與評語不會重改，短答仍須手動批改。各項操作見 [八項新增功能與升級步驟](./TEACHING_UPGRADE.md)。

## 資料與私隱

網站會在學生裝置的 `localStorage` 保存班別、姓名、學號、任務進度與尚未送出的同步佇列。教師應依校本私隱政策告知學生資料用途、保留期及查詢方式；不再需要的成績分頁應由獲授權教師在試算表內處理。


### MC 自動鼓勵評語

MC 即時結果及「我的提交與評語」設有獨立的「MC 自動回饋」，顯示答中題數、MC 得分與一句固定的廣東話鼓勵評語；未經教師工作室可信核算前標示為練習參考。系統使用 110 句固定文案，每 10% 一組，按答中 MC 題數除以 MC 總題數分組，不使用加權總分或非 MC 成績。非整十比例向下分組；大於 0% 而不足 10% 使用 10 組，0 與 100 組只限全錯與全對。非精確十位比例不會抽到宣稱精確比例的句子。

同次作答以提交識別碼固定選句，重新查看或老師批改短答不會改變 MC 評語。未核算的舊版提交若與現有網站題目版本不同，會等原版本核算後才顯示回饋。純 MC 只供教師查閱自動回饋，不需再填老師評語；混合卷的 MC 回饋只針對 MC 部分，非 MC 題目仍由老師評分並填評語。純非 MC 不顯示 MC 回饋。既有老師評語保留，不會被自動句子覆蓋。
